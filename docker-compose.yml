services:
  subconv-next:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: subconv-next
    restart: unless-stopped
    ports:
      - "9876:9876"
    volumes:
      - ./config:/config
      - ./data:/data
